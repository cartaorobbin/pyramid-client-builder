---
description: Ensures the dev-workflow skill is considered at the start of every chat
alwaysApply: true
---

# Dev Workflow Check

At the start of every new conversation, **always** ask the user whether this work should follow the dev-workflow skill.

Use AskQuestion:


If the user says yes, read and follow `.cursor/skills/dev-workflow/SKILL.md` **before** doing anything else.

If the user says no, proceed normally.

**Never skip this question. Never try to auto-detect. Always ask.**